---
description: Implement the next unchecked user story with tests
---
Read commands/implement.md from the agentic-engineering skill, then follow those instructions.

**Additional constraint:** Always run tests with `vitest run` (or `npx vitest run`), never bare `vitest` or `npx vitest`. Bare vitest enters watch mode — its worker processes outlive the Bash tool timeout and accumulate as orphaned Node processes.
