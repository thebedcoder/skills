---
description: Plan, apply, and review a non-bug change — new shortcut, new format support, faster path, or a refactor
argument-hint: <what to improve> [--auto]
---
Read `${CLAUDE_PLUGIN_ROOT}/skills/agentic-engineering/commands/improve.md` — that file holds the real instructions; this wrapper holds none.

Follow it, for improvement: $ARGUMENTS

It references policy sections of `SKILL.md` and blocks of `shared/preamble.md`, both under `${CLAUDE_PLUGIN_ROOT}/skills/agentic-engineering/`. Read those when it points you there — the wrapper does not load them for you.
