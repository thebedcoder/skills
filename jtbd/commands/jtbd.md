---
description: Run the Jobs to Be Done megaskill — research, personas, competitors, landing page copy, ad scripts
argument-hint: "[product brief or MODE 0-4]"
---
Invoke the `jtbd` skill (via the Skill tool) and pass through these arguments verbatim: $ARGUMENTS

The skill's SKILL.md handles routing — empty args prints the MODE GUIDE, a product brief starts MODE 0, an explicit `MODE N` jumps to that mode.
